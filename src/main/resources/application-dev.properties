# Email Configuration
mail.default.address=ap9645605@gmail.com
mail.google.app.password=kjfe dgrv suzq tbma

# PostgreSQL Database Configuration
# On Render, set DB_URL / DB_USER / DB_PASSWORD environment variables to override these defaults
db.url=jdbc:postgresql://localhost:5432/acebank
db.user=postgres
db.password=
db.script.path=/sql/schema_initializer.sql
db.driver=org.postgresql.Driver

# SMTP Server Settings
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.smtp.connection-timeout=5000
mail.smtp.timeout=5000
